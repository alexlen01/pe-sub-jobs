<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.1.0</version>
    <relativePath/>
  </parent>

  <groupId>com.ubs.lending</groupId>
  <artifactId>pe-sub-jobs</artifactId>
  <version>1.1.5</version>
  <name>pe-sub-jobs</name>

  <properties>
    <java.version>25</java.version>
  </properties>

  <dependencies>

    <!-- DB-less: no jdbc starter, no JDBC driver, no embedded test database. All writes go
         through pe-sub-api's REST endpoints; Spring Batch runs on ResourcelessJobRepository. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-batch</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Boot 4.x modularized test support: MockMvc auto-configuration
         (org.springframework.boot.webmvc.test.autoconfigure) lives here, not in starter-test. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webmvc-test</artifactId>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.springframework.batch</groupId>
      <artifactId>spring-batch-test</artifactId>
      <scope>test</scope>
    </dependency>

  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>

      <!-- Resolve the mockito-core jar path into the ${org.mockito:mockito-core:jar} property so
           Surefire can load it as a -javaagent below. Versions are managed by
           spring-boot-starter-parent; the `properties` goal binds to the initialize phase, before
           tests run. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <goals>
              <goal>properties</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <!-- Load Mockito as an explicit Java agent instead of letting it self-attach at runtime;
           self-attachment is deprecated and will stop working on future JDKs. -Xshare:off silences
           the CDS "sharing is only supported for boot loader classes" warning that the appended
           agent otherwise triggers. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <argLine>-javaagent:${org.mockito:mockito-core:jar} -Xshare:off</argLine>
        </configuration>
      </plugin>
    </plugins>
  </build>

</project>
